Agente, o plano final para a sincronização multimodal foi aprovado. A missão é implementar um sistema de upload robusto, com limites claros e feedback para o usuário, garantindo a compatibilidade total com a nova arquitetura do backend.
Sua Missão Definitiva:
Adicione um Estado para Controle de Arquivo: Se ainda não existir, adicione um estado para o arquivo em si: const [selectedFile, setSelectedFile] = useState<File | null>(null);
Modifique a Lógica de Upload: Atualize o componente de upload (FileUploader ou a lógica dentro de App.tsx) para aceitar apenas um arquivo por vez, com um limite de tamanho de 10MB. Se um arquivo maior for selecionado, exiba um alerta para o usuário.
Substitua a Função de Envio (handleSubmit): Substitua completamente a função handleSubmit no arquivo src/App.tsx pelo código multimodal final, que agora lida com o File object diretamente para máxima performance e envia os dados como FormData.
Generated typescript
// Em src/App.tsx, substitua a função handleSubmit por esta:

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();

  const textToAnalyze = userText.trim();
  if (!selectedFile && !textToAnalyze) {
    alert("Por favor, forneça um texto ou selecione um arquivo para análise.");
    return;
  }
  
  if (loading) return;

  setLoading(true);
  setResult(null);
  console.log("🔍 Iniciando análise multimodal...");

  const formData = new FormData();
  formData.append('question', specificQuestion.trim());

  if (selectedFile) {
    formData.append('file', selectedFile);
  } else {
    const textBlob = new Blob([textToAnalyze], { type: 'text/plain' });
    formData.append('file', textBlob, 'input_manual.txt');
  }

  try {
    // A chamada da API agora usa FormData
    const response = await fetch(`${BACKEND_BASE_URL}/api/analyze`, {
      method: 'POST',
      body: formData, // O navegador definirá o Content-Type correto
    });

    if (!response.ok) {
      const errorText = await response.text();
      throw new Error(`Erro do Servidor: ${response.status} - ${errorText}`);
    }

    const data = await response.json();
    setResult(data);
    // (Lógica de histórico, se for re-implementada no futuro)

  } catch (error: unknown) {
    console.error("❌ Falha na análise:", error);
    let errorMessage = (error instanceof Error) ? error.message : "Ocorreu um erro desconhecido.";
    setResult({ displayData: { humanized_text: `Falha na Análise: ${errorMessage}` } });
  } finally {
    setLoading(false);
  }
};
